import XCTest
@testable import ios_glia_core

final class ios_glia_coreTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ios_glia_core().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
